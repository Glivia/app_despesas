name: app_despesas
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  syncfusion_flutter_charts: ^27.1.58

  # Supabase dependency (adicionando o Supabase Flutter)
  supabase_flutter: ^2.6.0 # Atualize conforme a versão mais recente disponível no pub.dev

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
